#include<iostream>
#define MAX 100
int find(char* s1, char* s2);
using namespace std;
int find(char *s1, char *s2) {
	
}
int main() {

	char str[100];
	char str2[] = "are";
	int c1 = find(str, str2)
}