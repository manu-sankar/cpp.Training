#include<iostream>
using namespace std;
int main() {
	int capStorage, capCurrent, capMax;
	capMax = 100;
	capCurrent = capMax - capStorage;
	if (capStorage == 0) {
		cout << "Storage is full";
		return 0;
	}
cout << "You can store more files";
}